{ { { { { var sera = 'Será??' } } } } }
console.log(sera) // com var, a variável é vísivel em todos os blocos de código. É global.

function George () {
    var a = 4
    console.log(a)
} // variáveis var dentro de função permanecem só nesse escopo.

